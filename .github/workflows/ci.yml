name: CI Pipeline

on:
  push:
    branches: [main, master]
  pull_request:
    branches: [main, master]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout código
        uses: actions/checkout@v4

      - name: Validar documentación básica
        run: |
          echo "📄 Validando README"
          test -f README.md || echo "⚠️ README pendiente"

      - name: Validar páginas de error (Next.js App Router)
        run: |
          echo "🚨 Validando páginas de error"
          test -f src/app/error.tsx || echo "⚠️ error.tsx pendiente"
          test -f src/app/not-found.tsx || echo "⚠️ not-found.tsx pendiente"

      - name: Validar estructura de rutas
        run: |
          echo "🗂️ Validando estructura de rutas"
          test -d src/app/about || echo "⚠️ Ruta /about pendiente"
          test -d src/app/registro || echo "⚠️ Ruta /registro pendiente"
          test -d src/app/contact || echo "⚠️ Ruta /contact pendiente"

      - name: Build Docker image
        run: docker build -t gestion-citas .